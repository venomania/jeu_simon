{% extends 'base.html.twig' %}

{% block title %}Joueur{% endblock %}

{% block body %}
    <h1>Joueur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ joueur.id }}</td>
            </tr>
            <tr>
                <th>Useur</th>
                <td>{{ joueur.useur }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ joueur.score }}</td>
            </tr>
            <tr>
                <th>Temps</th>
                <td>{{ joueur.temps }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ joueur.date ? joueur.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('joueur_index') }}">back to list</a>

    <a href="{{ path('joueur_edit', {'id': joueur.id}) }}">edit</a>

    {{ include('joueur/_delete_form.html.twig') }}
{% endblock %}
